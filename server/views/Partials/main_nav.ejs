<!-- Centered Header -->
<header class="header-section">
  <h1 class="text-center">Tournament Bracket Manager</h1>
</header>

<!-- Centered NavBar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark justify-content-center">
  <div class="container">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mx-auto">
        <li class="nav-item">
          <% if(title=="Home") { %>
            <a class="nav-link active" href="/home">
          <% } else {%>
            <a class="nav-link" href="/home">
          <% } %>
          <i class="fa-solid fa-house-chimney-user"></i> Home</a>
        </li>
        <li class="nav-item">
          <% if(title=="About us") { %>
            <a class="nav-link active" href="/aboutus">
          <% } else {%>
            <a class="nav-link" href="/aboutus">
          <% } %>
          <i class="fa-solid fa-circle-info"></i> About Us</a>
        </li>
        <li class="nav-item">
          <% if(title=="Products") { %>
            <a class="nav-link active" href="/products">
          <% } else {%>
            <a class="nav-link" href="/products">
          <% } %>
          <i class="fa-brands fa-elementor"></i> Products</a>
        </li>
        <li class="nav-item">
          <% if(title=="Service") { %>
            <a class="nav-link active" href="/service">
          <% } else {%>
            <a class="nav-link" href="/service">
          <% } %>
          <i class="fa-solid fa-gears"></i> Service</a>
        </li>
        <li class="nav-item">
          <% if(title=="Tournaments") { %>
            <a class="nav-link active" href="/tournaments">
          <% } else {%>
            <a class="nav-link" href="/tournaments">
          <% } %>
          <i class="fa-solid fa-trophy"></i> Tournaments</a>
        </li>
        <li class="nav-item">
          <% if(title=="Contact Us") { %>
            <a class="nav-link active" href="/contactus">
          <% } else {%>
            <a class="nav-link" href="/contactus">
          <% } %>
          <i class="fa-solid fa-address-card"></i> Contact Us</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
